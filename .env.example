# AI-Demo Environment Variables
# Copy this file to .env and modify values as needed.
# WARNING: Never commit .env to source control.

# ──────────────────────────────────────────────
# PostgreSQL (Application Database)
# ──────────────────────────────────────────────
POSTGRES_DB=aidemo
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# Connection String (for reference)
# Host=localhost;Port=5432;Database=aidemo;Username=postgres;Password=postgres

# ──────────────────────────────────────────────
# Authentik (OIDC Provider - Local Development)
# ──────────────────────────────────────────────
AUTHENTIK_SECRET_KEY=this-is-a-development-secret-key-change-in-production
AUTHENTIK_BOOTSTRAP_PASSWORD=admin
AUTHENTIK_BOOTSTRAP_EMAIL=admin@localhost
AUTHENTIK_BOOTSTRAP_TOKEN=dev-bootstrap-token

# Authentik PostgreSQL (separate from app DB)
AUTHENTIK_POSTGRESQL__USER=authentik
AUTHENTIK_POSTGRESQL__PASSWORD=authentik-devpass
AUTHENTIK_POSTGRESQL__NAME=authentik

# ──────────────────────────────────────────────
# PRODUCTION NOTES:
# ──────────────────────────────────────────────
# For production, you MUST change:
#   - POSTGRES_PASSWORD (use a strong, generated password)
#   - AUTHENTIK_SECRET_KEY (use a long random string, e.g. openssl rand -hex 32)
#   - AUTHENTIK_BOOTSTRAP_PASSWORD (use a strong admin password)
#   - AUTHENTIK_POSTGRESQL__PASSWORD (use a strong, generated password)
#
# For production OIDC (Azure AD/Auth0/etc), configure these in appsettings:
#   - Oidc:Authority  (e.g. https://login.microsoftonline.com/{tenant}/v2.0)
#   - Oidc:ClientId   (your application's client ID)
#   - Oidc:Audience   (your API audience identifier)
#
# Enable HTTPS:
#   - Set RequireHttpsMetadata=true in JWT bearer options
#   - Use proper TLS certificates
